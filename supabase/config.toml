# Edge Functions: default gateway JWT verification can return 401 for valid sessions after
# migrating to ECC (P-256) JWT signing keys. See:
# https://supabase.com/docs/guides/functions/auth
#
# parse-receipt verifies the caller with supabase.auth.getUser() inside the function instead.
[functions.parse-receipt]
verify_jwt = false
